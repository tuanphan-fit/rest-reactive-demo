quarkus:
  datasource:
    db-kind: postgresql
    password: 1234
    reactive:
      url: jdbc:postgresql://localhost:5432/quarkus
  hibernate-orm:
    database:
        generation: drop and create
    log:
      format-sql: true
      sql: true
  jackson:
    fail-on-empty-beans: false
